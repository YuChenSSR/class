# DECISIONS — 项目决策记录

> 按 quantum-repo-to-paper 流程要求维护。每条含日期、决策、理由与影响范围。

## 2026-07-05 — Gate 决策：本轮定位为"项目改善"，不做发表建议

- **用户输入**：已读体检报告；确认本工作为其已发表论文（CLASS, ICCAD 2025）；
  明确指示**不再给任何发表/选会建议**，只回答"项目有没有可以改善的地方"。
  同时指示继续 Phase 4–6 的机制（证据构建、可复现性加固、写作骨架），无人值守。
- **无人值守解释**（用户方向模板 `<proceed / 按差异化轴 X 推进>` 未填写）：
  改善轴取体检报告第 3/5 节中与发表决策无关的工程与科学质量项，按证据价值排序：
  1. S-2 的受控消融（布局阶段 DM0 tie-break 开/关）——回答"控制器感知布局阶段到底贡献多少"；
  2. S-1 死旋钮修复（评估器参数化，`--t` 真实生效）——恢复器件演进敏感性分析能力；
  3. 多种子方差——补单种子无误差棒的缺口（最小规模）；
  4. 可复现性加固：KL mapper 确定性选项、Fig 7/8 改为 CSV 派生、每图/表一条命令；
  5. 写作骨架与摘要草稿：**会场中立**（不提任何 venue/deadline），仅作为扩展手稿素材。
- **实验预算**：单条命令 ≤ 30 分钟；完整规模不跑，命令写入 `docs/reproduction-commands.md`；
  不伪造、不外推完整规模结果；未覆盖的主张标 NOT YET SUPPORTED。
- **矩阵签核**：Phase 4 要求用户签核实验矩阵；无人值守模式下跳过签核，矩阵与执行状态
  记录在 `docs/reproduction-commands.md` 与 `docs/PROGRESS.md`，用户可事后追认或否决。

## 2026-07-05 — 代码改动原则：opt-in、默认行为逐字节不变

- 所有为消融/敏感性分析新增的开关（`--layout-heuristic`、`--t-eval`、KLMapper 确定性参数）
  默认关闭；默认路径以回归验证（pe_20 输出与审计基准逐字节比对 + 全量 pytest）证明不变。
- **不修改已发表算法本身**：S-2 的坐标系问题本轮只做消融取证，不改 Rust 打分代码——
  改动会使仓库结果偏离已发表论文，是否修留给作者决定（证据在手后成本很低）。
